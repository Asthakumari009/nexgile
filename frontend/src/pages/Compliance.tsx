import { fmt, useCompliance } from '../api'

export function Compliance() {
  const readiness = useCompliance()
  if (readiness.error) return <p className="border border-red-200 bg-red-50 p-4 text-sm text-red-800">Could not load readiness indicators: {(readiness.error as Error).message}</p>
  return <div className="space-y-5"><div><p className="text-xs font-medium text-sky-800">Disclosure data</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Reporting readiness</h1><p className="mt-1 text-sm text-slate-500">Readiness is calculated from inventory completeness, primary-source coverage, approvals, and retained lineage.</p></div><section className="border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">These are data-readiness indicators. They are not a regulatory compliance determination.</section><div className="grid gap-4 lg:grid-cols-2">{(readiness.data ?? []).map((item) => <section key={item.framework} className="border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><h2 className="text-sm font-semibold">{item.name}</h2><p className="mt-1 text-xs text-slate-500">Based on the current reporting inventory</p></div><p className="num text-2xl font-semibold text-sky-900">{fmt(item.readiness_pct, 0)}%</p></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><p className="text-xs font-medium text-emerald-800">Available</p><ul className="mt-2 space-y-2 text-xs leading-relaxed text-slate-600">{item.met.map((line) => <li key={line}>{line}</li>)}</ul></div><div><p className="text-xs font-medium text-amber-800">Needs attention</p><ul className="mt-2 space-y-2 text-xs leading-relaxed text-slate-600">{item.unmet.map((line) => <li key={line}>{line}</li>)}</ul></div></div><a href={`/api/v1/compliance/${item.framework}/export`} className="mt-5 inline-block rounded border border-slate-300 px-3 py-2 text-sm font-medium text-slate-700 transition hover:border-sky-400 hover:text-sky-800 active:translate-y-px">Export readiness CSV</a></section>)}</div></div>
}
